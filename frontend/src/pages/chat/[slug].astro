---
import Layout from '@layouts/Layout.astro';
import ChatContainer from '@/components/chat/ChatContainer.jsx';
import '@styles/global.css';

let { slug } = Astro.params;
---

<Layout title={`Chat`}>
  <ChatContainer client:load id={slug}/>
</Layout>