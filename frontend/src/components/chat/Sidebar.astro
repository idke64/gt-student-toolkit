---
const { activeSlug } = Astro.props;
---

<aside class="w-64 bg-white border-r border-gray-200 p-4">
  <div class="mb-6">
    <a 
      href="/chat/new" 
      class={`block w-full text-center bg-yellow-400 hover:bg-yellow-500 text-black py-2 px-4 rounded-lg shadow-md transition-colors ${
        !activeSlug ? 'ring-2 ring-yellow-600' : ''
      }`}
    >
      + New Chat
    </a>
  </div>

  <div class="flex justify-between items-center mb-4">
    <h2 class="text-lg font-semibold">Conversations</h2>
  </div>

  <nav class="space-y-2">
    <a
      href="/chat/gt-barcelona"
      class={`flex items-center gap-3 p-2 rounded-lg ${
        activeSlug === 'gt-barcelona' 
          ? 'bg-yellow-100 text-yellow-800 border-l-4 border-yellow-400' 
          : 'hover:bg-gray-100'
      }`}
    >
      <span>GT Barcelona</span>
    </a>
  </nav>
</aside>