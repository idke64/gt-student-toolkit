---
const { text, type } = Astro.props;
---

<div class={`p-3 rounded-lg max-w-[75%] ${
  type === 'user' 
    ? 'bg-gt-primary text-white ml-auto' 
    : 'bg-gray-100 text-gt-dark mr-auto'
} ${
  type === 'important' 
    ? 'border-l-4 border-gt-accent bg-yellow-50'
    : ''
}`}>
  {text}
</div>