---
const { title, type } = Astro.props;
---

<div class={`p-4 rounded-lg ${
  type === 'info' 
    ? 'bg-white border border-gray-200' 
    : 'bg-gt-light border-l-4 border-gt-accent'
}`}>
  {title && <h3 class="font-semibold text-gt-secondary mb-2">{title}</h3>}
  <slot />
</div>